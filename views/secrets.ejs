<%- include('partials/header') %>

<div class="jumbotron text-center">
        <div class="container">
            <i class="fas fa-user-secret fa-6x"></i>
            <h1 class="display-3">Você descobriu um segredo!</h1>

            <% for(let secret of secrets){ %>
                <p class="secret-text"><%= secret.secret %> </p>
            <% } %> 
            <hr>
            <a class="btn btn-outline-info btn-lg" href="/logout" role="button">Sair</a>
            <a class="btn btn-dark btn-lg" href="/submit" role="button">Compartilhe um segredo</a>
        </div>
    </div>

<%- include('partials/footer') %>